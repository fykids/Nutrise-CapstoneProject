<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Nutrise" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="colorOnPrimary">@color/ColorPrimary</item>
        <item name="colorOnSecondary">@color/ColorSecondary</item>
        <item name="colorOnBackground">@color/ColorDark</item>
    </style>

    <style name="TextColor">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="IconStyle">
        <item name="android:tint">@color/white</item>
    </style>

    <style name="ColorContainer">
        <item name="colorOnBackground">#343434</item>
    </style>
</resources>