<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Nutrise" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <item name="colorOnPrimary">@color/ColorPrimary</item>
        <item name="colorOnSecondary">@color/ColorSecondary</item>
        <item name="colorOnBackground">@color/white</item>
    </style>

    <style name="TextColor">
        <item name="android:textColor">@color/ColorDark</item>
    </style>

    <style name="IconStyle">
        <item name="color">@color/ColorDark</item>
    </style>

    <style name="ColorContainer">
        <item name="colorOnBackground">@color/white</item>
    </style>

    <style name="SlideActivityAnimation" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/slide_in_right</item>
        <item name="android:activityOpenExitAnimation">@anim/slide_out_left</item>
    </style>

    <style name="Theme.Nutrise" parent="Base.Theme.Nutrise" />
</resources>